{
  "name": "@stevvvns/device-accounts",
  "version": "0.0.1",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist/index.js",
    "dist/stores/accounts.js",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "mkdir -p dist; cp src/index.html dist/; npx esbuild --bundle --keep-names --format=esm --platform=browser --outdir=dist src/index.js src/stores/accounts.js src/example-app.js",
    "example": "npm run build && npx serve ./dist; npx prettier src --write",
    "pub": "npm run build && npm publish --access=public",
    "lint": "npx eslint --fix src"
  },
  "author": "",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "@stevvvns/incomponent": "^0.0.20",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.0",
    "eslint": "^9.39.0",
    "globals": "^16.5.0"
  }
}
